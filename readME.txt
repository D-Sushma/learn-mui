1.. create app name "learn-mui"
(install - npm install @material-ui/core) --> this is not installed , so rather than this install below package.
2.. install "npm install @mui/material @emotion/react @emotion/styled"
3.. Remove previous code from "App.js" & simply add "message that you want". 
4.. Check on browser "npm start"

//** Button
-lots of variant & lots of button type in mui 
- mui.com-> go component -> Button
1. use Button --> <Button></Button> 
2. property --> 
        ~ variant - will change layout & it can change the background as well as supporter
                ->text(normal), outlined, contained
        ~ color - primary, info, secondary, warning, success, error
        ~ size - small, medium, large
        ~ border
        ~ disabled 
        ~ disableElevation
        ~ for link --> href="#text-buttons"
3. icon with button 
       -for icon --> " npm install @mui/icons-material "
4. click on button
5. change color on button click

//** ButtonGroup
1. use
2. properties
3. orientation - vertical, horizonatal